{% extends 'baseBack.html.twig' %}

{% block title %} Ajouter un évènement {% endblock %}

{% block body %}
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <h1>Ajouter un évènement</h1>

    {{ include('admin/formevent.html.twig') }}

    <a href="{{ path('app_admin') }}">retour</a>
     {{ form_rest(form) }}
{% endblock %}