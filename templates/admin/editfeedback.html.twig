{% extends 'baseBack.html.twig' %}

{% block title %}Modifier Feedback{% endblock %}

{% block body %}
    <div class="container mt-5">
        

        <div class="card p-4 shadow">
            {{ include('admin/feedbackform.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>

        <a href="{{ path('app_admin') }}" class="btn btn-secondary mt-3">
            Retour
        </a>
    </div>
 
 <div class="deldiv">
    {{ include('feedback/_delete_form.html.twig') }}
    </div>

    <style>
    .deldiv{
        margin-top:-38px;
        margin-left:110px;
    }
    </style>
{% endblock %}
