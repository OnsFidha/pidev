<nav class="navbar navbar-example navbar-expand-lg bg-light">
	<div class="container-fluid">
		{# <a class="navbar-brand" href="{{path('app_homepage')}}">Artistool</a> #}
	<div id="preloader">
            
        </div>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-ex-3">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbar-ex-3">
			<div class="navbar-nav me-auto">
			<img src={{asset('img/logo.png')}}  width="75" height="50"> 
				<a class="nav-item nav-link" href="{{path('app_homepage')}}">Home</a>
				{# <a class="nav-item nav-link" href="javascript:void(0)">Gestion1</a>
				<a class="nav-item nav-link" href="javascript:void(0)">Gestion2</a>
				<a class="nav-item nav-link" href="javascript:void(0)">Gestion3</a>
				<a class="nav-item nav-link" href="javascript:void(0)">Gestion4</a>
				<a class="nav-item nav-link" href="javascript:void(0)">Gestion5</a> #}
			</div>
			{% if app.user %}
				<ul class="navbar-nav">
					<li class="nav-item navbar-dropdown dropdown-user dropdown">
						<a class="nav-link dropdown-toggle hide-arrow" href="javascript:void(0);" data-bs-toggle="dropdown" aria-expanded="true">
							<div class="avatar">
								<img src="../assets/pdp/{{app.user.image}}" alt="" class="w-px-40 h-auto rounded-circle">
							</div>
						</a>
						<ul class="dropdown-menu dropdown-menu-end" data-bs-popper="none">
							<li>
								<a class="dropdown-item" href="{{path('app_profile')}}">
									<div class="d-flex">
										<div class="flex-shrink-0 me-3">
											<div class="avatar">
												<img src="../assets/pdp{{app.user.image}}" alt="" class="w-px-40 h-auto rounded-circle">
											</div>
										</div>
										<div class="flex-grow-1">
											<span class="fw-semibold d-block">{{app.user.name}}
												{{app.user.prename}}</span>
											<small class="text-muted">
												{% for role in app.user.roles %}
													{% if role == "ROLE_USER" %}User
													{% elseif role == "ROLE_ADMIN" %}Admin
													{% endif %}
													{% if not loop.last %},
													{% endif %}
												{% endfor %}
											</small>
										</div>
									</div>
								</a>
							</li>
							<li>
								<div class="dropdown-divider"></div>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_profile') }}">
									<i class="bx bx-user me-2"></i>
									<span class="align-middle">My Profile</span>
								</a>
							</li>
							{% if is_granted('ROLE_ADMIN') %}
							<li>
								<a class="dropdown-item" href="./admin/html/dashboard.php">
									<i class="bx bxs-dashboard me-2"></i>
									<span class="align-middle">Dashboard</span>
								</a>
							</li>
							{% endif %}
							<li>
								<a class="dropdown-item" href="{{ path('app_edit_profile', {'id': app.user.id}) }}">
									<i class="bx bx-cog me-2"></i>
									<span class="align-middle">Settings</span>
								</a>
							</li>
							<li>
								<div class="dropdown-divider"></div>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_logout') }}">
									<i class="bx bx-power-off me-2"></i>
									<span class="align-middle">Log Out</span>
								</a>
							</li>
						</ul>
					</li>
				</ul>
			{% else %}
				<button class="btn btn-outline-primary" type="button">Login</button>
			{% endif %}
		</div>
	</div>
</nav>

