{% extends 'base.html.twig' %}

{% block title %}Participation{% endblock %}

{% block body %}
    <h1>Participation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participation.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_particip_index') }}">back to list</a>

    <a href="{{ path('app_particip_edit', {'id': participation.id}) }}">edit</a>

    {{ include('particip/_delete_form.html.twig') }}
{% endblock %}
