

{# <img src="../../public/img/logo.png"> #}
<img src={{asset('img/logo.png')}}  width="250" height="200">

<p >  <center>Liste des Reclamations</center>

</p>
<div >
    <table align="center" border="1">
        <thead>
        <tr>

            <th> type </th>
            <th> Description</th>
            <th> Date </th>
            <th> Etat </th>

        </tr>
        </thead>
        <tbody>
        {% for r in reclamations %}
            <tr>
                <td> {{ r.type}} </td>
                <td> {{ r.description }} </td>
                <td> {{ r.dateCreation ? r.dateCreation|date('Y-m-d') : ''  }} </td>
                <td> {{ r.etat ? 'Traitée' : 'Non traitée' }} </td>

            </tr>
        {% endfor %}


        </tr>
        </tbody>
    </table>
</div>