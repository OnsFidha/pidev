{% extends 'base.html.twig' %}

{% block title %}New Reclamation{% endblock %}

{% block body %}
    <h1>Create new Reclamation</h1>

    {# {{ include('reclamation/_form.html.twig') }}

    <a href="{{ path('app_reclamation_index') }}">back to list</a> #}

    {{form_start(form,{'attr':{'novalidate':'novalidate'}})}}

<table>
<tr>
<td> {{form_label(form.type,"type de reclamation")}}</td>
<td> {{form_widget(form.type)}} </td>
<td> {{form_errors(form.type)}} </td>
</tr>

<tr>
<td> {{form_label(form.description,"Description de la réclamation")}}</td>
<td> {{form_widget(form.description)}} </td>
<td> {{form_errors(form.description)}} </td>
</tr>


</table>
{{form_widget(form.save)}}
{{form_end(form)}}
<a href="{{ path('app_reclamation_index') }}">back to list</a> 
{% endblock %}
