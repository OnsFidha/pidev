

{# <img src="../../public/img/logo.png"> #}
{# <img src={{asset('img/logo.png')}}  width="250" height="200"> #}
<p >  <center>Liste des Utilisateurs</center>

</p>
<div >
    <table align="center" border="1">
       <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Name</th>
                <th>Prename</th>
                <th>arts</th>
                {# <th>Password</th> #}
                <th>Phone</th>
                <th>IsVerified</th>
                <th>Birthday</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.name }}</td>
                <td>{{ user.prename }}</td>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                {# <td>{{ user.password }}</td> #}
                <td>{{ user.phone }}</td>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
                <td>{{ user.birthday ? user.birthday|date('Y-m-d') : '' }}</td>
                <td><img src="{{ asset('assets/pdp/' ~ user.image) }}" alt="User Image"></td>

{% endfor %}

        </tr>
        </tbody>
    </table>
</div>