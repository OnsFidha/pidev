{% extends 'base.html.twig' %}

{% block title %}WhatsappNotif{% endblock %}

{% block body %}
    <h1>WhatsappNotif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ whatsapp_notif.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ whatsapp_notif.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_whatsapp_notif_index') }}">back to list</a>

    <a href="{{ path('app_whatsapp_notif_edit', {'id': whatsapp_notif.id}) }}">edit</a>

    {{ include('whatsapp_notif/_delete_form.html.twig') }}
{% endblock %}
